# Database (used by docker-compose and PostgreSQL)
DB_USER=synapse
DB_PASSWORD=change_me
DB_NAME=synapse

# JWT signing key (Base64-encoded secret for HMAC)
JWT_SECRET_KEY=change_me_to_a_base64_secret
